import { AnimationTriggerMetadata } from '@angular/core';
/**
 * Animates element to move and fade in on enter and move and fade out on leave
 *
 * @export
 * @returns
 */
export declare function moveIn(): AnimationTriggerMetadata;
/**
 * Animates element to fall and fade in on enter and fall and fade out on leave
 *
 * @export
 * @returns
 */
export declare function fallIn(): AnimationTriggerMetadata;
/**
 * Animates element to fade in on enter and fade out on leave
 *
 * @export
 * @returns {AnimationTriggerMetadata}
 */
export declare function fadeInOut(): AnimationTriggerMetadata;
/**
 * Animates element to grow on enter and shrink on leave
 *
 * @export
 * @returns
 */
export declare function growShrink(): AnimationTriggerMetadata;
